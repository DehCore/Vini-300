/* Reset básico */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: 'Inter', sans-serif;
}

body {
    background-color: #f5f6fa;
    color: #333;
    line-height: 1.5;
}

/* Container */
.container {
    width: 90%;
    max-width: 1200px;
    margin: 0 auto;
}

/* Dashboard Layout */
.dashboard-layout {
    display: flex;
    flex-direction: column;
    min-height: 100vh;
}

/* Header */
.dashboard-header {
    background-color: #fff;
    padding: 20px 0;
    border-bottom: 1px solid #ddd;
}

.header-content {
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.header-info h1 {
    font-weight: 600;
    font-size: 24px;
}

.header-info p {
    font-size: 14px;
    color: #555;
}

.header-actions button {
    margin-left: 10px;
}

/* Buttons */
.btn {
    padding: 8px 16px;
    border: none;
    cursor: pointer;
    border-radius: 6px;
    font-weight: 500;
    display: inline-flex;
    align-items: center;
    gap: 5px;
}

.btn-primary {
    background-color: #4a90e2;
    color: #fff;
}

.btn-ghost {
    background-color: transparent;
    color: #4a90e2;
    border: 1px solid #4a90e2;
}

.btn-outline {
    background-color: transparent;
    color: #333;
    border: 1px solid #ccc;
}

/* Stats Grid */
.stats-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: 20px;
    margin: 20px 0;
}

.stat-card {
    background-color: #fff;
    border-radius: 10px;
    padding: 20px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    box-shadow: 0 2px 6px rgba(0,0,0,0.1);
}

.stat-card.primary { border-left: 5px solid #4a90e2; }
.stat-card.success { border-left: 5px solid #28a745; }
.stat-card.secondary { border-left: 5px solid #6c757d; }
.stat-card.warning { border-left: 5px solid #ffc107; }

.stat-label {
    font-size: 14px;
    color: #888;
}

.stat-value {
    font-size: 22px;
    font-weight: 600;
    margin: 5px 0;
}

.stat-change.positive { color: #28a745; }
.stat-change.negative { color: #dc3545; }

/* Level and Quick Actions */
.level-actions-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 20px;
    margin-bottom: 20px;
}

.level-card-full {
    background-color: #fff;
    border-radius: 10px;
    padding: 20px;
    height: 100%;
    box-shadow: 0 2px 6px rgba(0,0,0,0.1);
}

/* Quick Actions */
.quick-actions h3 {
    margin-bottom: 10px;
    font-size: 18px;
}

.action-card {
    background-color: #fff;
    border-radius: 10px;
    padding: 15px;
    margin-bottom: 10px;
    box-shadow: 0 2px 6px rgba(0,0,0,0.1);
}

.action-content {
    display: flex;
    align-items: center;
    gap: 15px;
}

.action-icon {
    font-size: 24px;
    padding: 10px;
    border-radius: 50%;
    color: #fff;
}

.action-icon.primary { background-color: #4a90e2; }
.action-icon.warning { background-color: #ffc107; }
.action-icon.luxury { background-color: #8e44ad; }

.progress-bar {
    width: 100%;
    height: 8px;
    background-color: #eee;
    border-radius: 4px;
    margin-top: 10px;
}

.progress-fill {
    height: 100%;
    background-color: #4a90e2;
    border-radius: 4px;
}

/* Calendar Section */
.calendar-section {
    margin: 20px 0;
}

.calendar-section h2 {
    font-size: 20px;
    margin-bottom: 10px;
}

/* Tabs */
.tabs {
    background-color: #fff;
    border-radius: 10px;
    padding: 15px;
    box-shadow: 0 2px 6px rgba(0,0,0,0.1);
}

.tabs-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 10px;
}

.tabs-list {
    display: flex;
    gap: 10px;
}

.tab-trigger {
    background-color: #f0f0f0;
    border: none;
    padding: 8px 12px;
    border-radius: 6px;
    cursor: pointer;
}

.tab-trigger.active {
    background-color: #4a90e2;
    color: #fff;
}

.tab-content {
    display: none;
}

.tab-content.active {
    display: block;
}

.reservations-list {
    max-height: 200px;
    overflow-y: auto;
}
